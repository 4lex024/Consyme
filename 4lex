local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Rayfield Example Window",
   Icon = 0, -- Icon in Topbar. Can use Lucide Icons (string) or Roblox Image (number). 0 to use no icon (default).
   LoadingTitle = "Rayfield Interface Suite",
   LoadingSubtitle = "by Sirius",
   ShowText = "Rayfield", -- for mobile users to unhide rayfield, change if you'd like
   Theme = "Default", -- Check https://docs.sirius.menu/rayfield/configuration/themes

   ToggleUIKeybind = "K", -- The keybind to toggle the UI visibility (string like "K" or Enum.KeyCode)

   DisableRayfieldPrompts = false,
   DisableBuildWarnings = false, -- Prevents Rayfield from warning when the script has a version mismatch with the interface

   ConfigurationSaving = {
      Enabled = true,
      FolderName = nil, -- Create a custom folder for your hub/game
      FileName = "Big Hub"
   },

   Discord = {
      Enabled = false, -- Prompt the user to join your Discord server if their executor supports it
      Invite = "noinvitelink", -- The Discord invite code, do not include discord.gg/. E.g. discord.gg/ ABCD would be ABCD
      RememberJoins = true -- Set this to false to make them join the discord every time they load it up
   },

   KeySystem = false, -- Set this to true to use our key system
   KeySettings = {
      Title = "Untitled",
      Subtitle = "Key System",
      Note = "No method of obtaining the key is provided", -- Use this to tell the user how to get a key
      FileName = "Key", -- It is recommended to use something unique as other scripts using Rayfield may overwrite your key file
      SaveKey = true, -- The user's key will be saved, but if you change the key, they will be unable to use your script
      GrabKeyFromSite = false, -- If this is true, set Key below to the RAW site you would like Rayfield to get the key from
      Key = {"Hello"} -- List of keys that will be accepted by the system, can be RAW file links (pastebin, github etc) or simple strings ("hello","key22")
   }
})

local MainTab = Window:CreateTab("Home", 4483362458) 

-- ESP Bonnie Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Bonnie (Merah)",
	CurrentValue = false,
	Flag = "ToggleBonnieESP",
	Callback = function(Value)
		ESPBonnieEnabled = Value

		if ESPBonnieEnabled then
			print("ESP Bonnie Aktif")
			-- Create or enable highlight for Bonnie
			task.spawn(function()
				local function updateESP()
					local bonniePath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Bonnie")
					
					if bonniePath then
						-- Check if highlight already exists
						local existingHighlight = bonniePath:FindFirstChild("BonnieESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "BonnieESP"
							highlight.Adornee = bonniePath
							highlight.FillColor = Color3.fromRGB(255, 0, 0) -- Merah
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0) -- Merah
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = bonniePath
							print("ESP Highlight dibuat untuk Bonnie")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Bonnie tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Bonnie Nonaktif")
			-- Disable highlight for Bonnie
			task.spawn(function()
				local bonniePath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Bonnie")
				if bonniePath then
					local existingHighlight = bonniePath:FindFirstChild("BonnieESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})

-- ESP Chica Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Chica (Merah)",
	CurrentValue = false,
	Flag = "ToggleChicaESP",
	Callback = function(Value)
		ESPChicaEnabled = Value

		if ESPChicaEnabled then
			print("ESP Chica Aktif")
			task.spawn(function()
				local function updateESP()
					local chicaPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Chica")
					
					if chicaPath then
						local existingHighlight = chicaPath:FindFirstChild("ChicaESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "ChicaESP"
							highlight.Adornee = chicaPath
							highlight.FillColor = Color3.fromRGB(255, 0, 0)
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = chicaPath
							print("ESP Highlight dibuat untuk Chica")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Chica tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Chica Nonaktif")
			task.spawn(function()
				local chinaPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Chica")
				if chinaPath then
					local existingHighlight = chinaPath:FindFirstChild("ChicaESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})

-- ESP Cupcake Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Cupcake (Merah)",
	CurrentValue = false,
	Flag = "ToggleCupcakeESP",
	Callback = function(Value)
		ESPCupcakeEnabled = Value

		if ESPCupcakeEnabled then
			print("ESP Cupcake Aktif")
			task.spawn(function()
				local function updateESP()
					local cupcakePath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Cupcake")
					
					if cupcakePath then
						local existingHighlight = cupcakePath:FindFirstChild("CupcakeESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "CupcakeESP"
							highlight.Adornee = cupcakePath
							highlight.FillColor = Color3.fromRGB(255, 0, 0)
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = cupcakePath
							print("ESP Highlight dibuat untuk Cupcake")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Cupcake tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Cupcake Nonaktif")
			task.spawn(function()
				local cupcakePath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Cupcake")
				if cupcakePath then
					local existingHighlight = cupcakePath:FindFirstChild("CupcakeESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})

-- ESP Foxy Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Foxy (Merah)",
	CurrentValue = false,
	Flag = "ToggleFoxyESP",
	Callback = function(Value)
		ESPFoxyEnabled = Value

		if ESPFoxyEnabled then
			print("ESP Foxy Aktif")
			task.spawn(function()
				local function updateESP()
					local foxyPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Foxy")
					
					if foxyPath then
						local existingHighlight = foxyPath:FindFirstChild("FoxyESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "FoxyESP"
							highlight.Adornee = foxyPath
							highlight.FillColor = Color3.fromRGB(255, 0, 0)
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = foxyPath
							print("ESP Highlight dibuat untuk Foxy")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Foxy tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Foxy Nonaktif")
			task.spawn(function()
				local foxyPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Foxy")
				if foxyPath then
					local existingHighlight = foxyPath:FindFirstChild("FoxyESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})

-- ESP Freddy Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Freddy (Merah)",
	CurrentValue = false,
	Flag = "ToggleFreddyESP",
	Callback = function(Value)
		ESPFreddyEnabled = Value

		if ESPFreddyEnabled then
			print("ESP Freddy Aktif")
			task.spawn(function()
				local function updateESP()
					local freddyPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Freddy")
					
					if freddyPath then
						local existingHighlight = freddyPath:FindFirstChild("FreddyESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "FreddyESP"
							highlight.Adornee = freddyPath
							highlight.FillColor = Color3.fromRGB(255, 0, 0)
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = freddyPath
							print("ESP Highlight dibuat untuk Freddy")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Freddy tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Freddy Nonaktif")
			task.spawn(function()
				local freddyPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Freddy")
				if freddyPath then
					local existingHighlight = freddyPath:FindFirstChild("FreddyESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})

-- ESP Puppet Toggle
local Toggle = MainTab:CreateToggle({
	Name = "ESP Puppet (Merah)",
	CurrentValue = false,
	Flag = "TogglePuppetESP",
	Callback = function(Value)
		ESPPuppetEnabled = Value

		if ESPPuppetEnabled then
			print("ESP Puppet Aktif")
			task.spawn(function()
				local function updateESP()
					local puppetPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Puppet")
					
					if puppetPath then
						local existingHighlight = puppetPath:FindFirstChild("PuppetESP")
						if not existingHighlight then
							local highlight = Instance.new("Highlight")
							highlight.Name = "PuppetESP"
							highlight.Adornee = puppetPath
							highlight.FillColor = Color3.fromRGB(255, 0, 0)
							highlight.OutlineColor = Color3.fromRGB(255, 0, 0)
							highlight.FillTransparency = 0.5
							highlight.OutlineTransparency = 0
							highlight.Parent = puppetPath
							print("ESP Highlight dibuat untuk Puppet")
						else
							existingHighlight.Enabled = true
						end
					else
						warn("Puppet tidak ditemukan di lokasi yang dimaksud")
					end
				end
				
				updateESP()
			end)
		else
			print("ESP Puppet Nonaktif")
			task.spawn(function()
				local puppetPath = workspace:FindFirstChild("Game"):FindFirstChild("Animatronics"):FindFirstChild("Animatronics"):FindFirstChild("Puppet")
				if puppetPath then
					local existingHighlight = puppetPath:FindFirstChild("PuppetESP")
					if existingHighlight then
						existingHighlight.Enabled = false
					end
				end
			end)
		end
	end,
})
